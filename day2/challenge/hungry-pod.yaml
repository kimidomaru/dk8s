apiVersion: v1
kind: Pod
metadata:
  labels:
    run: hungry-pod
  name: hungry-pod
  namespace: labch2
spec:
  containers:
  - name: hungry-pod
    command: ["stress"]
    args:
      - --vm
      - "1"
      - --vm-bytes
      - 250M
      - --vm-hang
      - "1"
    image: polinux/stress
    resources: 
      limits:
        memory: 200Mi
      requests:
        memory: 100Mi