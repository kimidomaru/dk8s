apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    pvc: first-pvc
  name: my-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: giropops
  selector:
    matchLabels:
      storage: slow_spd
